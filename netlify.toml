[build]
  publish = "exampleSite/public"
  command = "cd exampleSite/ && hugo --gc --minify --themesDir ../.."

[build.environment]
HUGO_VERSION = "0.96.0"
HUGO_ENABLEGITINFO = "true"
HUGO_THEME = "repo"
HUGO_BASEURL = "/"
